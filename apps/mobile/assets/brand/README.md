# Brand assets

- `logo.png` — optional ShopHeader mark.
- Until you have a real logo, `BrandMark` shows a letter **N** (`USE_BRAND_LOGO_PNG = false` in `components/ui/BrandMark.tsx`).
- When ready: replace this PNG with a square logo, then set `USE_BRAND_LOGO_PNG = true` and reload Metro.
